DESCRIPTION
Before commiting changes, format and check the code with ‘npm run lint’, and run
the tests with ‘npm t’. NO WARNINGS PERMITTED!

SERVER {{{
	When developing on the backend, start the server with ‘npm start’ and watch
	for code changes with ‘npm run watch’. Then open your browser at the address
	displayed on the console.

	- npm start
}}}

DATABASE {{{
	- npm run dbc
	- use log2990
	- db.createUser({ user: "foo", pwd: "bar", roles: [{ db: "log2990", role: "readWrite" }], mechanisms: ["SCRAM-SHA-256"] })
}}}

NAMING {{{
	- observables end with $
	  (angular.io/guide/rx-library#naming-conventions-for-observables)
	- All hex values in caps
}}}

IMPORTS {{{
	PREFERED:
	- import { X } from 'X'
	LAST RESORT:
	- `import X from 'X'
	PROHIBITED:
	- import * as X from 'X'
	- const X = import('X')
	RESOURCES:
	- https://github.com/sourcegraph/javascript-typescript-langserver/pull/480#discussion_r197905711
	- https://github.com/Microsoft/TypeScript/issues/13340#issuecomment-348824479
	- https://github.com/DefinitelyTyped/DefinitelyTyped/issues/19337#issuecomment-364811834
	- https://github.com/microsoft/TypeScript/issues/16093
	- https://github.com/ng-packagr/ng-packagr/issues/1239
}}}

ABBREVIATIONS {{{
	- arg = argument
	- cap = capacity
	- ctx = context
	- el = element
	- err = error
	- ev = event
	- ex = exception
	- idx = index
	- len = length
	- msg = message
	- opt = optinal
	- pos = position
	- sz = size
	- val = value
}}}

KEYWORDS {{{
	- FIXME
	- NOTE
	- TODO
}}}

PONCTUATIONS {{{
	- ’ as apostrophe
	- “” for quotes
}}}

HTML {{{
	- Do not quote if number
	- Single quote if string or spaced numbers
	- All ‘div’ tags must be referenced by class (for CSS) or by id (for JS)
	- Self tags should end with / (e.g. <img />)
	- Images must have alt, height and width attributes
}}}

vim:fdm=marker:cc=80:tw=80:fo+=t:fo-=l
