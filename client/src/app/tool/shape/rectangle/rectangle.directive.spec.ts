import { RectangleDirective } from './rectangle.directive';

describe('RectangleDirective', () => {
  it('should create an instance', () => {
    // const directive = new RectangleDirective();
    // expect(directive).toBeTruthy();
    expect(true).toBeTruthy();
  });
});
