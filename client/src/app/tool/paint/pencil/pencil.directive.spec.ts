import { PencilDirective } from './pencil.directive';

describe('PencilDirective', () => {
  it('should create an instance', () => {
    const directive = new PencilDirective();
    expect(directive).toBeTruthy();
  });
});
