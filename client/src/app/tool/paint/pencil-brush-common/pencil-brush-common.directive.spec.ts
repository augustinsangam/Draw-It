import { PencilBrushCommonDirective } from './pencil-brush-common.directive';

describe('PencilBrushCommonDirective', () => {
  it('should create an instance', () => {
    const directive = new PencilBrushCommonDirective();
    expect(directive).toBeTruthy();
  });
});
